# cSelSplitN_modName.v

## 不一致的部分

### Release Rule（释放规则）不一致

#### JSON 中的定义（第 1-75 行）

```json
"release_rule": {
  "policy": "selected_only",
  "allowed_variants": ["selected_only", "custom_expr"],
  "details": "Selected channel's i_free[k] triggers upstream o_free"
}
```

**预期行为**：

- 策略为 `selected_only`
- 只有**被选中的下游通道**的 `i_free[k]` 才应触发上游 `o_free`
- 未被选中的通道的 `i_free` 信号应被忽略

#### Verilog 代码中的实现

```verilog
// 第 73 行
assign w_freeNext = i_free0 | i_free1;
```

**note**: 但因为只有一个通道会被fire，也只被fire的通道会传回，代码编写只是为了方便


**实际行为**：

- 使用 **OR 逻辑**，即：`w_freeNext = i_free0 | i_free1`
- **任一通道**的 `i_free` 都会触发上游 `o_free`
- 不论该通道是否被选中（`w_valid_n[k]` 是否为 1）

#### 问题分析

| 项目                   | JSON 预期                         | 代码实现               | 是否一致       |
| ---------------------- | --------------------------------- | ---------------------- | -------------- |
| 发送 `o_free` 的条件 | 仅当选中通道的 `i_free[k]` 为 1 | `i_free0 \| i_free1`  | ❌**否** |
| 复杂度                 | 需要选择器逻辑                    | 直接 OR 所有 free 信号 | ❌**否** |
| 流控语义               | 选中通道确认，才释放上游          | 任意下游确认就释放上游 | ❌**否** |

#### 可能的后果

- 未被选中的下游通道的 `i_free` 信号也会**错误地释放**上游事件
- 上游流控逻辑可能被破坏
- 不符合 SelSplit 组件的预期"选择性路由"语义

#### 修复建议

如果需要遵守 JSON 中的 `selected_only` 策略，应改为：

```verilog
// 方案：使用选择器逻辑
assign w_freeNext = (w_valid_n[0] & i_free0) | (w_valid_n[1] & i_free1);
```

或者，如果代码的设计是有意的（例如出于特定的硬件设计考虑），应在 JSON 中更新策略为 `broadcast_all` 或在 `EXTRA_PARAMS` 中添加说明。

---

# cFifo1_modName.v

## 不一致的部分

### 1. 缺少数据宽度参数

#### JSON 中的定义（第 77-123 行）

```json
"params": {
  "DATA_WIDTH": null,
  "EXTRA_PARAMS": {}
},
"payload": {
  "mode": "unknown",
  "width_param": "DATA_WIDTH",
  "data_in_port": null,
  "data_out_port": null
}
```

**预期行为**：

- 应该有 `DATA_WIDTH` 参数定义有效负载宽度
- 应该有 `data_in_port` 和 `data_out_port` 用于有效负载传输
- 支持有效负载缓存

#### Verilog 代码中的实现

```verilog
module cFifo1_modName (
	i_drive,
	i_freeNext,
	o_free,
	o_driveNext,
	o_fire_1,
	rstn
);
// 无 DATA_WIDTH 参数
// 无数据端口（data_in/data_out）
```

**实际行为**：

- 完全**没有**数据相关的参数和端口
- 只有事件信号（drive/free）
- 不支持任何有效负载传输

#### 问题分析

| 项目            | JSON 预期 | 代码实现 | 是否一致     |
| --------------- | --------- | -------- | ------------ |
| DATA_WIDTH 参数 | ✓ 有     | ❌ 无    | **否** |
| 数据输入端口    | ✓ 有     | ❌ 无    | **否** |
| 数据输出端口    | ✓ 有     | ❌ 无    | **否** |
| 有效负载缓存    | ✓ 可选   | ❌ 无    | **否** |

### 2. 实现细节差异

#### JSON 合约（预期模型）

```json
"invariants": [
  "Non-reentrant: the FIFO stage must not accept a new upstream drive until the previous transaction is freed.",
  "Upstream o_free is only generated after receiving the paired i_free (i_freeNext in staged designs), which indicates the transaction is complete.",
  "Payload may be cached (implementation may store/hold payload until completion)."
]
```

**预期**：

- 非重入性（FIFO 阶段阻止重新进入）
- 等待下游 free 后才释放上游
- 有效负载缓存支持

#### Verilog 代码中的实现

```verilog
// 使用了原始模块的组合
sender sender(...)           // 发送端
relay relay0(...)            // 中继
receiver receiver(...)       // 接收端
freeSetDelay #(.DELAY_UNIT_NUM(2)) outdelay0_donttouch (...)
freeSetDelay #(.DELAY_UNIT_NUM(2)) outdelay1_donttouch (...)
```

**实际**：

- 使用了具体的硬件原语（sender/relay/receiver）
- 通过 `freeSetDelay` 模块添加了额外的延时
- 只处理事件流，不处理有效负载

#### 问题分析

| 项目         | JSON 预期              | 代码实现     | 差异                  |
| ------------ | ---------------------- | ------------ | --------------------- |
| 抽象层级     | 功能合约（不指定实现） | 具体原语组合 | 实现方式完全不同      |
| 有效负载处理 | 支持缓存和传输         | 完全缺失     | ❌**不符合**    |
| 延时模型     | 未指定                 | 显式两级延时 | ✓ 可接受（实现细节） |

### 3. 功能不完整性

| 功能              | JSON 要求 | 代码支持                     | 状态              |
| ----------------- | --------- | ---------------------------- | ----------------- |
| 事件驱动传播      | ✓        | ✓                           | ✓ 完全支持       |
| 事件释放传播      | ✓        | ✓                           | ✓ 完全支持       |
| 非重入性保证      | ✓        | ✓（通过 sender/relay 实现） | ✓ 部分支持       |
| 有效负载输入/输出 | ✓        | ❌                           | **❌ 缺失** |
| 有效负载缓存      | ✓ 可选   | ❌                           | **❌ 缺失** |

## 总结

**cFifo1_modName.v 是一个"无有效负载版本"的 Fifo1 实现**，仅处理事件流控制，不处理任何数据有效负载。这与 JSON 模板中要求的 `DATA_WIDTH` 参数和数据端口完全不符。

### 修复建议

1. **选项 A**：扩展代码以支持有效负载

   - 添加 `DATA_WIDTH` 参数
   - 添加 `i_data` 和 `o_data` 端口
   - 在 relay 或缓存中保存有效负载
2. **选项 B**：更新 JSON 模板

   - 为 Fifo1 创建两个变体：`Fifo1_PayloadLess`（仅事件）和 `Fifo1_WithPayload`（含有效负载）
   - 或在 `EXTRA_PARAMS` 中标记："payload_support": false

---

# cPmtFifo1_modName.v

## 不一致的部分

### 1. 缺少数据宽度参数和数据端口

#### JSON 中的定义（第 125-163 行）

```json
"params": {
  "DATA_WIDTH": null,
  "EXTRA_PARAMS": {}
},
"permit": {
  "pmt_port": null,
  "active_level": "unknown"
},
"payload": {
  "mode": "unknown",
  "width_param": "DATA_WIDTH",
  "data_in_port": null,
  "data_out_port": null
}
```

**预期行为**：

- 应该有 `DATA_WIDTH` 参数定义有效负载宽度
- 应该有 `data_in_port` 和 `data_out_port` 用于有效负载传输
- 支持有效负载缓存
- 有 `pmt_port` 用于许可信号

#### Verilog 代码中的实现

```verilog
module cPmtFifo1_modName(
	i_drive,
	i_freeNext, 
	o_free,
	o_driveNext,
	o_fire_1,
	rstn,
	pmt
);
// 无 DATA_WIDTH 参数
// 无数据端口（data_in/data_out）
// 有 pmt 端口
```

**实际行为**：

- 完全**没有**数据相关的参数和端口
- 只有事件信号（drive/free）和许可信号（pmt）
- 不支持任何有效负载传输

#### 问题分析

| 项目            | JSON 预期         | 代码实现     | 是否一致       |
| --------------- | ----------------- | ------------ | -------------- |
| DATA_WIDTH 参数 | ✓ 有             | ❌ 无        | **否**   |
| 数据输入端口    | ✓ 有             | ❌ 无        | **否**   |
| 数据输出端口    | ✓ 有             | ❌ 无        | **否**   |
| 有效负载缓存    | ✓ 可选           | ❌ 无        | **否**   |
| Permit 端口     | ✓ 有（pmt_port） | ✓ 有（pmt） | ✓**是** |

### 2. Permit 语义不够明确

#### JSON 中的定义

```json
"permit_semantics": {
  "behavior_when_disabled": "stall",
  "notes": [
    "Stall means it pauses progression/forwarding; do not interpret as protocol change unless explicitly implemented."
  ]
}
```

**预期**：

- permit 被禁用时，阶段暂停转发（stall）
- 不改变协议本身，只是暂停

#### Verilog 代码中的实现

```verilog
(* dont_touch="true" *) pmtRelay pmtRelay0(
	.inR  (w_outRRelay_3[0]),
	.inA  (w_outARelay_3[0]),
	.outR (w_outRRelay_3[1]),
	.outA (w_outARelay_3[1]),
	.fire (),
	.pmt  (pmt),
	.rstn (rstn)
);
```

**实际**：

- 使用了专门的 `pmtRelay` 原语处理 permit 信号
- 具体的 permit 行为由 `pmtRelay` 模块定义
- JSON 中未指定 `pmt` 的 active_level（高有效还是低有效？）

#### 问题分析

| 项目              | JSON 预期           | 代码实现               | 状态                    |
| ----------------- | ------------------- | ---------------------- | ----------------------- |
| Permit 暂停行为   | 明确定义为 stall    | 由 pmtRelay 实现       | ✓ 可能符合，但需要验证 |
| Active Level 说明 | unknown（需要推断） | 需要查看 pmtRelay 代码 | ⚠️**不明确**    |

### 3. 实现架构差异

#### JSON 预期的架构

```
upstream → [sender] → [stall_logic] → [relay] → [receiver] → downstream
           ↑                                          ↓
           └─────── free 反向传播 ──────────────────┘
```

#### 代码实现的架构

```
upstream → [sender] → [pmtRelay + permit] → [relay] → [receiver] → downstream
           ↑                                                    ↓
           └─────────── free 反向传播 ────────────────────────┘
```

**差异**：

- JSON 假设在普通 relay 之前有 stall 逻辑
- 代码使用专门的 `pmtRelay` 在第一个 relay 之后
- 两种架构对 permit 的控制点位置不同

### 4. 完整性对比

| 功能              | JSON 要求 | 代码支持                | 状态              |
| ----------------- | --------- | ----------------------- | ----------------- |
| 上游事件驱动传播  | ✓        | ✓                      | ✓ 完全支持       |
| 下游事件驱动传播  | ✓        | ✓                      | ✓ 完全支持       |
| 非重入性保证      | ✓        | ✓（通过 sender/relay） | ✓ 部分支持       |
| Permit 控制       | ✓        | ✓（通过 pmtRelay）     | ✓ 部分支持       |
| 有效负载输入/输出 | ✓        | ❌                      | **❌ 缺失** |
| 有效负载缓存      | ✓ 可选   | ❌                      | **❌ 缺失** |

## 总结

**cPmtFifo1_modName.v 是一个"无有效负载版本"的 PmtFifo1 实现**，仅处理事件流控制和 permit 控制，不处理任何数据有效负载。这与 JSON 模板中要求的 `DATA_WIDTH` 参数和数据端口完全不符。

### 修复建议

1. **选项 A**：扩展代码以支持有效负载

   - 添加 `DATA_WIDTH` 参数
   - 添加 `i_data` 和 `o_data` 端口
   - 在 relay 或缓存中保存有效负载
2. **选项 B**：更新 JSON 模板

   - 为 PmtFifo1 创建两个变体：`PmtFifo1_PayloadLess`（仅事件）和 `PmtFifo1_WithPayload`（含有效负载）
   - 在 `EXTRA_PARAMS` 中标记："payload_support": false
   - 明确指定 permit 的 active_level（从代码推断或文档说明）

---

# cNatSplitN_modName.v

### 1. 参数命名与数据宽度设计差异

#### JSON 中的定义（第 165-207 行）

```json
"params": {
  "NUM_PORTS": null,
  "DATA_WIDTH": null,
  "EXTRA_PARAMS": {}
},
"payload": {
  "mode": "unknown",
  "width_param": "DATA_WIDTH",
  "data_in_port": null
}
```

**预期**：

- 单一的 `DATA_WIDTH` 参数
- 所有下游通道共享相同的数据宽度
- 有效负载"可选"（可能不处理数据）

#### Verilog 代码中的实现

```verilog
parameter NUM_PORTS      = 3,
parameter DATA_WIDTHI    = 32,  // 输入数据宽度
parameter DATA_WIDTHOUT0 = 12,  // 通道 0 输出宽度
parameter DATA_WIDTHOUT1 = 32,  // 通道 1 输出宽度
parameter DATA_WIDTHOUT2 = 24;  // 通道 2 输出宽度

assign o_data0 = i_data[DATA_WIDTHOUT0-1:0];
assign o_data1 = i_data[DATA_WIDTHOUT1-1:0];
assign o_data2 = i_data[DATA_WIDTHOUT2-1:0];
```

**实际**：

- 使用 **3 个不同的数据宽度参数**：输入宽度 + 每通道的输出宽度
- 不同通道可有**不同的输出宽度**
- 实现了**数据切分/路由功能**（payload 从"可选"变为"必需"）

#### 问题分析

| 项目             | JSON 预期                      | 代码实现                           | 差异                     |
| ---------------- | ------------------------------ | ---------------------------------- | ------------------------ |
| 数据宽度参数数量 | 1 个（DATA_WIDTH）             | 4 个（DATA_WIDTHI + 3 个输出宽度） | ❌**不符合**       |
| 参数命名         | 通用的 DATA_WIDTH              | 特定的 DATA_WIDTHI/OUT0/1/2        | ❌**不符合**       |
| 各通道数据宽度   | 相同（由单一 DATA_WIDTH 确定） | 可不同                             | ❌**设计理念不同** |
| 数据处理         | 可选（"presence": "optional"） | 必需（切分 i_data 到多个输出）     | ⚠️**超越规范**   |

### 2. 通道数量的参数化问题

#### JSON 中的定义

```json
"channels": {
  "num_ports_param": "NUM_PORTS",
  "downstream": [
    { "index": 0, "drive_out": null, "free_in": null, "data_out": null }
  ]
}
```

**预期**：

- NUM_PORTS 是参数，可配置任意数量的通道

#### Verilog 代码中的实现

```verilog
// NUM_PORTS = 3（硬编码默认值）
output wire [NUM_PORTS-1:0]      o_driveNext_n,
// 但数据端口是硬编码的：
output wire [DATA_WIDTHOUT0-1:0] o_data0,
output wire [DATA_WIDTHOUT1-1:0] o_data1,
output wire [DATA_WIDTHOUT2-1:0] o_data2,
```

**实际**：

- 虽然 `o_driveNext_n` 是向量化的（参数化），但 `o_data0/1/2` 是**硬编码的 3 个端口**
- 不是真正的参数化设计，而是**固定为 3 通道**

#### 问题分析

| 项目         | JSON 预期             | 代码实现                                     | 是否一致              |
| ------------ | --------------------- | -------------------------------------------- | --------------------- |
| 通道数参数化 | ✓ NUM_PORTS 可任意值 | ⚠️ NUM_PORTS 参数存在，但数据端口固定 3 个 | **❌ 半参数化** |
| 灵活性       | 支持任意通道数        | 实际只能用 NUM_PORTS=3                       | ❌**不符合**    |

### 3. 有效负载映射方式的差异

#### JSON 中的定义

```json
"payload_rule": {
  "presence": "optional",
  "mapping": "payload may be replicated/sliced per channel; implementation-defined"
}
```

**预期**：

- 有效负载映射方式是"实现定义的"
- 可以是复制、切分或其他方式

#### Verilog 代码中的实现

```verilog
// 采用"切分"方式
assign o_data0 = i_data[DATA_WIDTHOUT0-1:0];
assign o_data1 = i_data[DATA_WIDTHOUT1-1:0];
assign o_data2 = i_data[DATA_WIDTHOUT2-1:0];
```

**实际**：

- 使用**切分（slice）方式**
- 每个通道从 i_data 的不同位范围中提取数据
- 这是 broadcast 中的一个特殊变体：**每通道接收数据的不同部分**

#### 问题分析

| 项目     | JSON 预期        | 代码实现                 | 状态                 |
| -------- | ---------------- | ------------------------ | -------------------- |
| 映射方式 | 实现定义（可选） | 切分方式                 | ✓ 在允许范围内      |
| 文档化   | 应标明采用的方式 | 代码明确，但 JSON 应补充 | ⚠️**需完善** |

### 4. 事件路由的完整性

#### JSON 中的 Broadcast 合约

```json
"invariants": [
  "Broadcast semantics: an upstream event is broadcast to ALL downstream channels (not selection-based).",
  "Upstream o_free must wait for ALL downstream frees (all-acks completion)."
]
```

#### Verilog 代码中的事件路由

```verilog
// 驱动广播
assign o_driveNext_n[tap_i] = i_drive;

// All-acks 释放逻辑
assign w_andReq = & w_Req_n;        // 所有通道的 free 请求的与
assign w_sendFree = w_dirveReq & w_andReq;

(* dont_touch="true" *) freeSetDelay #(
   .DELAY_UNIT_NUM ( 4 )
) delay_ofree_donttouch (
   .i_pulse ( w_sendFree ),
   .o_pulse ( o_free ),
   .rstn     ( rstn )
);
```

**分析**：

- ✓ 驱动广播：`o_driveNext_n[k] = i_drive` ← 符合
- ✓ All-acks 释放：`w_andReq = & w_Req_n` ← 符合
- ✓ 延时处理：`freeSetDelay` 添加延时到 o_free ← 实现细节

| 项目          | JSON 预期          | 代码实现              | 是否一致       |
| ------------- | ------------------ | --------------------- | -------------- |
| 驱动广播      | ✓ 所有通道        | ✓ 所有通道           | ✓**是** |
| All-acks 释放 | ✓ 等待所有 i_free | ✓ w_andReq & w_Req_n | ✓**是** |

## 总结

**cNatSplitN_modName.v 在事件路由语义上与 NatSplitN 模板一致，但在数据处理的参数化设计上存在偏差。**

### 主要问题

1. **参数设计过于具体** - 使用 DATA_WIDTHI/OUT0/1/2 而非通用的 DATA_WIDTH + flexible 映射
2. **通道数半参数化** - 虽然定义了 NUM_PORTS，但数据端口 o_data0/1/2 是硬编码的，实际只能 NUM_PORTS=3
3. **文档化不足** - JSON 中未明确说明采用"切分映射"方式处理数据

### 修复建议

#### 选项 A：完全参数化（推荐）

```verilog
parameter NUM_PORTS = 3;
parameter DATA_WIDTHI = 32;
parameter [NUM_PORTS*32-1:0] DATA_WIDTHOUT_VECTOR = {24, 32, 12};  // 打包所有输出宽度

genvar k;
generate
  for (k = 0; k < NUM_PORTS; k = k + 1) begin : gen_output
    localparam OUT_WIDTH = DATA_WIDTHOUT_VECTOR[k*32+31:k*32];
    assign o_data[k][OUT_WIDTH-1:0] = i_data[OUT_WIDTH-1:0];
  end
endgenerate
```

#### 选项 B：更新 JSON 模板

在 NatSplitN 中添加 EXTRA_PARAMS：

```json
"EXTRA_PARAMS": {
  "DATA_WIDTHOUT_VECTOR": "packed array of per-channel output widths (optional)",
  "payload_mapping_mode": "slice (default) | replicate | custom"
}
```

同时在 customization_points 中补充：

```json
"Payload routing can be data slicing (different channels get different bit ranges), replication (same data to all), or custom mappings depending on implementation."
```

#### 选项 C：代码简化（如果固定为 3 通道）

在模块注释中明确标注：

```verilog
// NOTE: This implementation is specialized for NUM_PORTS=3 with heterogeneous output widths.
// For general NatSplitN behavior with NUM_PORTS variations, use a different implementation.
```

---

# cWaitMergeN_modName.v

### 1. 输入端口设计：硬编码 vs 参数化

#### JSON 中的定义（第 209-247 行）

```json
"inputs": [
  { "index": 0, "drive_in": null, "free_out": null, "data_in": null }
],
```

**预期**：

- NUM_PORTS 个输入通道
- 输入端口应该是参数化的（可通过 generate 或向量)
- 支持任意数量的输入

#### Verilog 代码中的实现

```verilog
parameter NUM_PORTS = 3;

input wire i_drive0,i_drive1,i_drive2,
input wire [DATA_WIDTH_I0-1:0] i_data0,
input wire [DATA_WIDTH_I1-1:0] i_data1,
input wire [DATA_WIDTH_I2-1:0] i_data2,
input wire i_freeNext,

output wire o_free0,o_free1,o_free2,
```

**实际**：

- 驱动信号：硬编码为 3 个独立端口（i_drive0/1/2），而非向量 i_drive_n[2:0]
- 数据输入：硬编码为 3 个独立端口（i_data0/1/2），各自宽度不同
- 释放输出：硬编码为 3 个独立端口（o_free0/1/2），而非向量 o_free_n[2:0]

#### 问题分析

| 项目           | JSON 预期             | 代码实现           | 差异               |
| -------------- | --------------------- | ------------------ | ------------------ |
| 输入通道参数化 | ✓ NUM_PORTS 可任意值 | ❌ 硬编码 3 个端口 | **不符合**   |
| 驱动信号组织   | 向量化或 generate     | 独立端口           | ❌**不符合** |
| 释放输出组织   | 向量化或 generate     | 独立端口           | ❌**不符合** |
| 灵活性         | 支持 N 通道           | 只能 NUM_PORTS=3   | ❌**不符合** |

### 2. 参数设计：异构宽度 vs 通用宽度

#### JSON 中的定义

```json
"params": {
  "NUM_PORTS": null,
  "DATA_WIDTH": null,
  "EXTRA_PARAMS": {}
}
```

**预期**：

- 使用通用的 `DATA_WIDTH` 参数
- 所有输入/输出端口宽度由 DATA_WIDTH 确定
- 异构情况由 EXTRA_PARAMS 说明

#### Verilog 代码中的实现

```verilog
parameter NUM_PORTS     = 3,
parameter DATA_WIDTH_I0 = 1,
parameter DATA_WIDTH_I1 = 3,
parameter DATA_WIDTH_I2 = 3,
parameter DATA_WIDTH_O  = 10;
```

**实际**：

- 4 个完全独立的宽度参数
- 每个输入通道可以有**不同的宽度**（1, 3, 3）
- 输出宽度独立参数化（10）

#### 问题分析

| 项目     | JSON 预期                      | 代码实现                     | 状态                     |
| -------- | ------------------------------ | ---------------------------- | ------------------------ |
| 参数数量 | 2 个（NUM_PORTS + DATA_WIDTH） | 5 个（NUM_PORTS + 4 个宽度） | ❌**过度参数化**   |
| 参数命名 | 通用 DATA_WIDTH                | 特定的 DATA_WIDTH_I0/I1/I2/O | ❌**不符合**       |
| 异构宽度 | 可选（EXTRA_PARAMS）           | 必需且硬编码                 | ❌**设计理念不同** |

### 3. 事件逻辑实现细节

#### JSON 合约：All-inputs join

```json
"join_condition": { "policy": "all_inputs" }
```

#### Verilog 代码实现

```verilog
assign w_andReq = & w_req_n;
delay2U u_delay2U_donttouch(.inR(w_andReq), .outR(w_d_andReq), .rstn(rstn));
assign w_driveNext = w_andReq ^ w_d_andReq;  // 边沿检测
assign w_sendDrive = w_driveNext & w_andReq;
assign o_driveNext = w_sendDrive;
```

**分析**：

- ✓ 基本 all-inputs join 逻辑正确（w_andReq = & w_req_n）
- ✓ 使用延时和异或进行边沿检测
- ✓ 确保只在所有输入准备好时才发送 o_driveNext
- ⚠️ 使用了额外的延时处理逻辑（delay2U），超出 JSON 规范范围

| 项目            | 说明                 | 状态          |
| --------------- | -------------------- | ------------- |
| All-inputs join | w_andReq = & w_req_n | ✓ 符合       |
| 边沿检测        | 使用 delay + XOR     | ⚠️ 实现细节 |

### 4. 释放逻辑实现

#### JSON 合约：Broadcast from output free

```json
"release_rule": {
  "policy": "broadcast_from_output_free",
  "details": "When output free arrives, free_out[k] are issued together for all k"
}
```

#### Verilog 代码实现

```verilog
assign w_sendFree  = i_freeNext;
assign o_free0     = i_freeNext;
assign o_free1     = i_freeNext;
assign o_free2     = i_freeNext;
```

**分析**：

- ✓ 所有 o_free[k] 同时等于 i_freeNext
- ✓ 广播释放信号到所有输入
- ✓ 实现了 broadcast_from_output_free 的合约

| 项目     | JSON 预期                 | 代码实现                 | 是否一致       |
| -------- | ------------------------- | ------------------------ | -------------- |
| 释放广播 | 所有 free_out[k] 一起发送 | o_free0/1/2 = i_freeNext | ✓**是** |

### 5. 数据合并映射

#### JSON 规范

```json
"payload_rule": {
  "presence": "optional",
  "mapping": "data_out formation is implementation-defined (concat/mux/etc.); must be evidenced or annotated"
}
```

#### Verilog 实现

```verilog
/*****modify as you wish*****/
assign o_data = {i_data2,i_data1,i_data0};
/*************************/
```

**分析**：

- ✓ 使用 concatenation（拼接）方式合并数据
- ✓ 代码中有明确注释说明是"自定义可修改的"
- ✓ 映射方式在允许的范围内（实现定义）

| 项目     | JSON 预期      | 代码实现                    | 状态             |
| -------- | -------------- | --------------------------- | ---------------- |
| 数据合并 | 实现定义       | {i_data2, i_data1, i_data0} | ✓**符合** |
| 文档化   | 应说明映射方式 | 有注释                      | ✓**有**   |

## 总结

**cWaitMergeN_modName.v 在事件join和释放逻辑上与WaitMergeN模板一致，但在输入端口的参数化设计上存在重大偏差。**

### 主要问题

1. **输入端口完全硬编码** - 驱动和数据输入是独立的 i_drive0/1/2 和 i_data0/1/2，而非向量化，导致只能固定 NUM_PORTS=3
2. **参数过度设计** - 使用 5 个独立参数（NUM_PORTS + DATA_WIDTH_I0/I1/I2 + DATA_WIDTH_O）而不是通用的 NUM_PORTS + DATA_WIDTH
3. **不符合规范** - JSON 期望通过 NUM_PORTS 支持任意数量的输入，但代码实现只能 NUM_PORTS=3
4. **异构宽度设计** - 虽然代码支持不同输入宽度，但这应在 EXTRA_PARAMS 中明确说明

### 修复建议

#### 选项 A：完全参数化（推荐）

```verilog
parameter NUM_PORTS = 3;
parameter DATA_WIDTH_IN = 8;   // 默认输入宽度
parameter DATA_WIDTH_OUT = 24;  // 默认输出宽度

// 向量化输入端口
input wire [NUM_PORTS-1:0] i_drive_n,
input wire [NUM_PORTS*DATA_WIDTH_IN-1:0] i_data_n,  // 打包的数据

// 向量化输出端口
output wire [NUM_PORTS-1:0] o_free_n,

// 使用 generate 处理异构宽度（如果需要）
genvar k;
generate
  for (k = 0; k < NUM_PORTS; k = k + 1) begin : gen_input_logic
    // 处理每个输入通道
  end
endgenerate
```

#### 选项 B：更新 JSON 模板

在 WaitMergeN 的 EXTRA_PARAMS 中添加：

```json
"EXTRA_PARAMS": {
  "DATA_WIDTH_IN_VECTOR": "optional packed array of per-input data widths (for heterogeneous merge)",
  "DATA_WIDTH_OUT": "output data width (auto-computed or custom)",
  "payload_merge_mode": "concat (default) | custom"
}
```

同时补充到 customization_points：

```json
"Input data widths can be homogeneous (single DATA_WIDTH) or heterogeneous (per-input width) depending on implementation. Specify in EXTRA_PARAMS if heterogeneous."
```

#### 选项 C：代码文档化

在模块注释中明确标注：

```verilog
// NOTE: This implementation is specialized for NUM_PORTS=3 with heterogeneous input widths.
// For parametric WaitMergeN supporting arbitrary NUM_PORTS, use a generate-based variant.
// 
// Mapping: o_data = {i_data2, i_data1, i_data0}  (concatenation order)
```

---

# cArbMergeN_modName.v

### 1. 输入端口设计：硬编码 vs 参数化

#### JSON 中的定义（第 249-291 行）

```json
"inputs": [
  { "index": 0, "drive_in": null, "free_out": null, "data_in": null }
]
```

**预期**：

- NUM_PORTS 个输入通道
- 支持任意数量的输入端口
- 端口应该是向量化或通过 generate 生成

#### Verilog 代码中的实现

```verilog
parameter NUM_PORTS  = 3;
parameter DATA_WIDTH = 32;

input                   i_drive0,
input  [DATA_WIDTH-1:0] i_data0,
output                  o_free0,

input                   i_drive1,
input  [DATA_WIDTH-1:0] i_data1,
output                  o_free1,

input                   i_drive2,
input  [DATA_WIDTH-1:0] i_data2,
output                  o_free2,
```

**实际**：

- 驱动信号：硬编码为 3 个独立端口（i_drive0/1/2），而非向量 i_drive_n[2:0]
- 数据输入：硬编码为 3 个独立端口（i_data0/1/2），而非向量
- 释放输出：硬编码为 3 个独立端口（o_free0/1/2），而非向量
- NUM_PORTS 虽为参数，但输入端口完全硬编码

#### 问题分析

| 项目           | JSON 预期             | 代码实现           | 差异               |
| -------------- | --------------------- | ------------------ | ------------------ |
| 输入通道参数化 | ✓ NUM_PORTS 可任意值 | ❌ 硬编码 3 个端口 | **不符合**   |
| 驱动信号组织   | 向量化或 generate     | 独立端口           | ❌**不符合** |
| 释放输出组织   | 向量化或 generate     | 独立端口           | ❌**不符合** |
| 灵活性         | 支持 N 通道           | 只能 NUM_PORTS=3   | ❌**不符合** |

### 2. 仲裁策略未明确文档化

#### JSON 规范

```json
"arbitration_policy": {
  "policy": "implementation_defined",
  "allowed_variants": ["fixed_priority", "round_robin", "custom"]
}
```

**预期**：

- 策略是实现定义的
- 但应在代码注释中明确说明采用的具体策略

#### Verilog 代码实现

```verilog
assign w_grant = w_req & (~w_req + 1'b1);
```

**实际**：

- 这是**最低优先级仲裁**（Lowest Priority Grant）
- 当多个请求同时到达时，选择最低的通道（通道 0 优先级最低）
- 实现采用位运算：`w_req & (~w_req + 1'b1)` 用来提取最低位的 1
- 代码中**没有注释说明仲裁策略**

#### 问题分析

| 项目     | JSON 预期      | 代码实现           | 状态                   |
| -------- | -------------- | ------------------ | ---------------------- |
| 策略定义 | 实现定义       | 最低优先级         | ✓ 在允许范围内        |
| 文档化   | 应在代码中说明 | 无注释             | ❌**缺失**       |
| 可推断性 | 从代码应能推断 | 需要深入分析位运算 | ⚠️**不易理解** |

### 3. 释放逻辑的复杂设计

#### JSON 合约：Selected-only release

```json
"release_rule": {
  "policy": "selected_only",
  "details": "Only the granted input receives completion back (free_out) per transaction"
}
```

**预期**：

- 只有被授予的输入接收 free_out
- 实现方式应相对直接

#### Verilog 代码实现

```verilog
generate
  for (i = 0; i < NUM_PORTS; i = i + 1) begin : arbUnit
    assign w_pf1if[i] = w_pf2of[i];
    assign w_pf2if[i] = i_freeNext & w_grant[i];
  
    cPmtFifo1 PmtFifo1 (...);
    cPmtFifo1 PmtFifo2 (
      .pmt(w_grant[i]),  // 使用 w_grant 作为 permit
      ...
    );
  
    freeSetDelay #(.DELAY_UNIT_NUM(DELAY2)) delay2 (
      .i_pulse (w_grant[i] & i_freeNext),
      .o_pulse (w_reset[i]),
      .rstn    (rstn)
    );
  end
endgenerate
```

**分析**：

- 使用**两级 cPmtFifo1 管道**来实现仲裁
- 第二级 PmtFifo2 的 permit 是 w_grant[i]
- w_pf2if[i] = i_freeNext & w_grant[i]，确保只有被授予的输入接收 free
- 使用额外的延时处理（freeSetDelay）来生成 w_reset 信号
- 实现超出基本规范的复杂性

#### 问题分析

| 项目       | JSON 预期     | 代码实现         | 差异                   |
| ---------- | ------------- | ---------------- | ---------------------- |
| 释放规则   | Selected-only | ✓ 实现正确      | ✓ 符合                |
| 实现复杂度 | 相对简单      | 两级管道 + 延时  | ⚠️**过度设计** |
| 清晰度     | 应易于理解    | 需要追踪多个信号 | ⚠️**难以推断** |

### 4. 参数配置的限制

#### 代码中的额外参数

```verilog
parameter NUM_PORTS  = 3,
parameter DATA_WIDTH = 32,
parameter DELAY1     = 1,
parameter DELAY2     = 1
```

**分析**：

- DELAY1 和 DELAY2 不在 JSON 规范中
- 这些是实现细节参数，应在 EXTRA_PARAMS 中说明
- JSON 中的 EXTRA_PARAMS 为空，但代码实际添加了这些参数

| 项目     | JSON 规范              | 代码实现       | 状态                         |
| -------- | ---------------------- | -------------- | ---------------------------- |
| 基本参数 | NUM_PORTS, DATA_WIDTH  | ✓ 有          | ✓ 符合                      |
| 扩展参数 | EXTRA_PARAMS（未定义） | DELAY1, DELAY2 | ⚠️**未在规范中说明** |

### 5. 数据选择逻辑

#### 实现方式

```verilog
assign o_data = ({DATA_WIDTH{w_grant[0]}} & r_data[0]) | 
                ({DATA_WIDTH{w_grant[1]}} & r_data[1]) | 
                ({DATA_WIDTH{w_grant[2]}} & r_data[2]);
```

**分析**：

- 使用 one-hot 译码的方式实现数据多选
- 逻辑正确，确保只有被授予的输入数据输出
- ✓ 符合规范要求

| 项目       | JSON 预期               | 代码实现    | 状态             |
| ---------- | ----------------------- | ----------- | ---------------- |
| 数据对应性 | 输出 = 被授予输入的数据 | ✓ 正确实现 | ✓**符合** |
| 实现方式   | 实现定义                | One-hot mux | ✓**合理** |

## 总结

**cArbMergeN_modName.v 在仲裁排他性和 selected-only 释放的核心逻辑上与 ArbMergeN 模板一致，但在输入端口参数化和设计文档化上存在重大问题。**

### 主要问题

1. **输入端口完全硬编码** - 使用 i_drive0/1/2、i_data0/1/2、o_free0/1/2 等独立端口，导致只能固定 NUM_PORTS=3，NUM_PORTS 参数形同虚设
2. **仲裁策略未文档化** - 虽然采用了最低优先级仲裁，但代码中没有注释说明，需要理解位运算才能推断
3. **设计过度复杂** - 使用两级 cPmtFifo1 管道 + 额外延时处理，超出基本规范的复杂度，增加了理解和维护难度
4. **参数不完整** - DELAY1/DELAY2 参数未在 JSON 中的 EXTRA_PARAMS 中说明

### 修复建议

#### 选项 A：完全参数化（推荐）

```verilog
parameter NUM_PORTS = 3;
parameter DATA_WIDTH = 32;
parameter ARBITRATION_POLICY = "lowest_priority";  // 新增说明
parameter DELAY1 = 1;
parameter DELAY2 = 1;

// 向量化输入端口
input wire [NUM_PORTS-1:0] i_drive_n,
input wire [NUM_PORTS*DATA_WIDTH-1:0] i_data_n,  // 打包的数据
output wire [NUM_PORTS-1:0] o_free_n,

// 向量化输出端口
output wire o_driveNext,
output wire [DATA_WIDTH-1:0] o_data,
input wire i_freeNext,

// 使用 generate 处理每个输入通道
genvar k;
generate
  for (k = 0; k < NUM_PORTS; k = k + 1) begin : gen_arb_unit
    // 处理每个仲裁单元
  end
endgenerate
```

#### 选项 B：更新 JSON 模板

在 ArbMergeN 中添加 EXTRA_PARAMS：

```json
"EXTRA_PARAMS": {
  "ARBITRATION_POLICY": "lowest_priority (default) | fixed_priority | round_robin | custom",
  "DELAY1": "optional delay in first pipeline stage",
  "DELAY2": "optional delay in second pipeline stage"
}
```

同时补充到 customization_points：

```json
"Implementation uses two-stage pipeline with PmtFifo1 for each input. Arbitration policy: lowest_priority (input 0 has lowest priority). Port packing is flexible; exclusivity guarantee is NOT."
```

#### 选项 C：补充代码注释

```verilog
// ========================================
// ArbMergeN 仲裁合并器
// ========================================
// 仲裁策略: 最低优先级 (Lowest Priority)
//   - 当多个请求同时到达时，选择通道编号最小的
//   - 实现: w_grant = w_req & (~w_req + 1'b1)
//
// 架构: 两级管道
//   - 第一级: PmtFifo1 用于数据缓存和请求握手
//   - 第二级: PmtFifo1 + permit 用于仲裁门控
//   - 延时: DELAY1 (第一级) 和 DELAY2 (第二级)
//
// 排他性: ✓ 保证同时只有一个输入被授予
// 释放规则: ✓ Selected-only (只有被授予的输入接收 free)
// ========================================

module cArbMergeN_modName #(
    parameter NUM_PORTS  = 3,
    parameter DATA_WIDTH = 32,
    parameter DELAY1     = 1,  // 第一级管道延时
    parameter DELAY2     = 1   // 第二级管道延时
) 
```

---

# cMutexMergeN_modName.v

### 1. 缺失 NUM_PORTS 参数

#### JSON 中的定义（第 293-331 行）

```json
"params": {
  "NUM_PORTS": null,
  "DATA_WIDTH": null,
  "EXTRA_PARAMS": {}
}
```

**预期**：

- NUM_PORTS 参数用于配置输入通道数
- 支持任意数量的输入（不限于 2）

#### Verilog 代码中的实现

```verilog
module cMutexMergeN_modName #(
    parameter DATA_WIDTH = 32
)(
    input                   i_drive0, i_drive1,
    input  [DATA_WIDTH-1:0] i_data0, i_data1,
    input                   i_freeNext,

    output                  o_free0,o_free1,
    output                  o_driveNext,
    output [DATA_WIDTH-1:0] o_data,
  
    input rstn
);
```

**实际**：

- ❌ **完全没有 NUM_PORTS 参数**
- 硬编码为 2 个输入（i_drive0/1, i_data0/1, o_free0/1）
- 这个模块实际是 "MutexMerge2"（两通道），不是通用的 N 通道

#### 问题分析

| 项目           | JSON 预期        | 代码实现         | 差异                 |
| -------------- | ---------------- | ---------------- | -------------------- |
| NUM_PORTS 参数 | ✓ 有            | ❌ 无            | **关键缺失**   |
| 通道数灵活性   | 任意 N 个        | 固定 2 个        | ❌**不符合**   |
| 模块范围       | 通用 MutexMergeN | 特定 MutexMerge2 | ❌**范围不符** |

### 2. 输入/输出端口硬编码

#### JSON 规范

```json
"inputs": [
  { "index": 0, "drive_in": null, "free_out": null, "data_in": null }
]
```

**预期**：

- NUM_PORTS 个输入通道
- 端口应该是向量化或通过 generate 生成

#### 代码实现

```verilog
input                   i_drive0, i_drive1,
input  [DATA_WIDTH-1:0] i_data0, i_data1,
output                  o_free0,o_free1,
```

**实际**：

- ❌ 硬编码为 2 个独立端口
- 不是向量化的（i_drive_n[1:0]）
- 输出释放信号也是独立的（o_free0, o_free1）

#### 问题分析

| 项目         | JSON 预期         | 代码实现    | 差异                     |
| ------------ | ----------------- | ----------- | ------------------------ |
| 输入通道组织 | 向量化或 generate | 独立端口    | ❌**不符合**       |
| 参数化程度   | NUM_PORTS 可配    | 硬编码 2 个 | ❌**完全不参数化** |

### 3. 互斥假设下的潜在问题

#### JSON 合约

```json
"invariants": [
  "Environment provides mutual exclusion: upstream inputs are assumed mutually exclusive.",
  "Conflicts are assumed NOT to arrive simultaneously; the component does not need to resolve simultaneous contention."
]
```

**预期**：

- 环境保证不会同时有多个 i_drive[k] 为 1
- 代码设计无需处理同时到达的冲突

#### Verilog 代码实现

```verilog
assign w_drive = i_drive0 | i_drive1;  // ← 这里处理可能的同时到达

assign w_data = (w_firstReq == 1'b1) ? i_data0 :
                ((w_secondReq == 1'b1) ? i_data1 : {DATA_WIDTH{1'b0}});  // ← 默认数据为 0
```

**分析**：

- ✓ w_drive = i_drive0 | i_drive1 在互斥条件下正确
- ✓ 数据选择逻辑在互斥条件下正确
- ⚠️ 但如果环境**违反互斥假设**（同时 i_drive0=1 和 i_drive1=1），代码会：
  - 产生 o_driveNext（因为 w_drive=1）
  - 选择 i_data0（因为条件优先级）
  - 这导致 i_data1 被忽略，违反语义

#### 问题分析

| 项目         | JSON 预期            | 代码实现        | 状态               |
| ------------ | -------------------- | --------------- | ------------------ |
| 互斥依赖     | 环境提供互斥         | ✓ 代码依赖环境 | ✓ 符合            |
| 互斥冲突处理 | 不处理（假设不发生） | 有隐含的优先级  | ⚠️**风险** |
| 文档化       | 应在注释中说明       | 无说明          | ❌**缺失**   |

### 4. 释放逻辑的条件设计

#### 实现方式

```verilog
assign w_firstFree  = w_freeNext & w_firstReq;
assign w_secondFree = w_freeNext & w_secondReq;

(* dont_touch="true" *) freeSetDelay #(
    .DELAY_UNIT_NUM(2)
) u_freeDelay0_donttouch (
    .i_pulse (w_firstFree), 
    .o_pulse (w_ofree_2[0]), 
    .rstn    (rstn)
);

(* dont_touch="true" *) freeSetDelay #(
    .DELAY_UNIT_NUM(2)
) u_freeDelay1_donttouch (
    .i_pulse (w_secondFree), 
    .o_pulse (w_ofree_2[1]), 
    .rstn    (rstn)
);

(* dont_touch="true" *) freeSetDelay #(.DELAY_UNIT_NUM(2)) outdelay0_donttouch 
    (.i_pulse(w_ofree_2[0]), .o_pulse(o_free0), .rstn(rstn));
(* dont_touch="true" *) freeSetDelay #(.DELAY_UNIT_NUM(2)) outdelay1_donttouch 
    (.i_pulse(w_ofree_2[1]), .o_pulse(o_free1), .rstn(rstn));
```

**分析**：

- ✓ w_firstFree = w_freeNext & w_firstReq 符合 selected-only 规则
- ✓ 只有当该输入活跃（w_firstReq=1）时，才释放它
- ⚠️ 但使用了两级 freeSetDelay（共 4 个时延单位），增加了复杂性
- 这超出了基本规范的描述

#### 问题分析

| 项目               | JSON 预期 | 代码实现 | 状态                   |
| ------------------ | --------- | -------- | ---------------------- |
| Selected-only 规则 | ✓ 实现   | ✓ 正确  | ✓**符合**       |
| 实现复杂度         | 相对简单  | 两级延时 | ⚠️**过度设计** |

### 5. 驱动信号延时

#### 实现方式

```verilog
freeSetDelay #(
    .DELAY_UNIT_NUM(4)
) u_driveDelay (
    .i_pulse (w_drive), 
    .o_pulse (o_driveNext), 
    .rstn    (rstn)
);
```

**分析**：

- 使用 4 个时延单位延迟驱动信号
- 这是实现细节，不在 JSON 规范中
- 可能用于与释放侧的延时同步

## 总结

**cMutexMergeN_modName.v 实际上是一个"硬编码 2 通道"的 MutexMerge 实现（应该叫 cMutexMerge2），而不是通用的 MutexMergeN。**

### 主要问题

1. **完全缺少 NUM_PORTS 参数** - 这是最关键的问题，模块根本无法参数化为 N 通道
2. **输入/输出端口硬编码** - 只支持 2 个输入，无法扩展
3. **不符合命名规范** - 模块名为 cMutexMergeN_modName，但实际是 2 通道实现，应该改为 cMutexMerge2_modName
4. **互斥假设风险未文档化** - 虽然代码依赖环境互斥，但如果违反假设会有隐含优先级行为
5. **实现超过规范** - 使用了多级延时处理，超出基本规范描述

### 修复建议

#### 选项 A：完全参数化（推荐）

```verilog
parameter NUM_PORTS = 2;
parameter DATA_WIDTH = 32;

// 向量化输入端口
input wire [NUM_PORTS-1:0] i_drive_n,
input wire [NUM_PORTS*DATA_WIDTH-1:0] i_data_n,  // 打包的数据

// 向量化输出端口
output wire [NUM_PORTS-1:0] o_free_n,

output wire o_driveNext,
output wire [DATA_WIDTH-1:0] o_data,
input wire i_freeNext,

// 使用 generate 处理每个输入
genvar k;
generate
  for (k = 0; k < NUM_PORTS; k = k + 1) begin : gen_mutex_unit
    // 处理每个输入通道的请求和释放逻辑
  end
endgenerate
```

#### 选项 B：更新文档，将其标记为 MutexMerge2

```verilog
// ========================================
// MutexMerge2 - 两通道互斥合并器
// ========================================
// NOTE: This is a specialized implementation for NUM_PORTS=2
//       It is NOT a generic MutexMergeN.
//
// 互斥假设: 环境保证 i_drive0 和 i_drive1 不会同时为 1
//
// 释放规则: Selected-only
//   - o_free0 仅在 i_freeNext=1 且该通道活跃时释放
//   - o_free1 仅在 i_freeNext=1 且该通道活跃时释放
//
// 数据选择: 优先级选择
//   - 如果违反互斥假设同时到达，i_data0 优先被选择
// ========================================
```

#### 选项 C：更新 JSON 模板中的 MutexMergeN

添加到 customization_points：

```json
"This implementation assumes environment-provided mutual exclusion strictly. All inputs must be mutually exclusive (never simultaneously driven). Ports and channel logic can be structured flexibly; NUM_PORTS can vary if properly generate-based or flatten-based."
```

同时在 EXTRA_PARAMS 中补充：

```json
"EXTRA_PARAMS": {
  "CONFLICT_BEHAVIOR": "if_simultaneous_violated (implementation may select one input by priority or other means)",
  "DELAY_DRIVE": "optional delay on output drive signal",
  "DELAY_FREE": "optional delay on free signal path"
}
```

```

```

3. **选项 C**：补充文档
   - 在 `customization_points` 中添加说明 permit 如何禁用 stall 转发
   - 文档化 pmtRelay 与标准 relay 的区别
